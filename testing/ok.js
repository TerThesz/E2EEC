const message_id = (data) => {
  // return a 10 character long string. Use first 10 characters of data. If there are not 10 characters, fill up all empty spaces with unique random chatacters

};

console.log(message_id('cc'), message_id('cc').length);